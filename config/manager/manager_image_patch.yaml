apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: cf-registry.nr-ops.net/mytu/cluster-api-aws-controller-amd64:dev
        name: manager
