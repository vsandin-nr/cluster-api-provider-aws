
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.8
  creationTimestamp: null
  name: awslaunchtemplates.exp.infrastructure.cluster.x-k8s.io
spec:
  group: exp.infrastructure.cluster.x-k8s.io
  names:
    kind: AWSLaunchTemplate
    listKind: AWSLaunchTemplateList
    plural: awslaunchtemplates
    singular: awslaunchtemplate
  scope: Namespaced
  versions:
  - name: v1alpha3
    schema:
      openAPIV3Schema:
        description: AWSLaunchTemplate is the Schema for the awslaunchtemplates API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AWSLaunchTemplateSpec defines the desired state of AWSLaunchTemplate
            properties:
              blockDeviceMappings:
                items:
                  description: BlockDeviceMappings
                  properties:
                    deviceName:
                      type: string
                    ebs:
                      description: EBS
                      properties:
                        encrypted:
                          type: string
                        volumeSize:
                          type: string
                        volumeType:
                          type: string
                      type: object
                  type: object
                type: array
              iamInstanceProfile:
                type: string
              imageId:
                description: 'todo: use a helper'
                type: string
              instanceType:
                description: 'InstanceType is the type of instance to create. Example:
                  m4.xlarge'
                type: string
              networkInterfaces:
                items:
                  description: NetworkInterface
                  properties:
                    deviceIndex:
                      type: string
                    groups:
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              sshKeyName:
                description: SSHKeyName is the name of the ssh key to attach to the
                  instance. Valid values are empty string (do not use SSH keys), a
                  valid SSH key name, or omitted (use the default SSH key name)
                type: string
              uncompressedUserData:
                description: UncompressedUserData specify whether the user data is
                  gzip-compressed before it is sent to ec2 instance. cloud-init has
                  built-in support for gzip-compressed user data user data stored
                  in aws secret manager is always gzip-compressed.
                type: boolean
            type: object
          status:
            description: AWSLaunchTemplateStatus defines the observed state of AWSLaunchTemplate
            properties:
              launchtemplateid:
                type: boolean
              version:
                type: boolean
            required:
            - launchtemplateid
            - version
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
